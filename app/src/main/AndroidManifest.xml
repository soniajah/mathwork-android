<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sonia.mathworkout">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".addsubActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".muldivActivity" />
        <activity android:name=".feelingcleverActivity" />
        <activity android:name=".StartActivity" />
        <activity android:name=".EndActivity" />
        <activity android:name=".CompleteActivity" />
        <activity android:name=".BraincruncherActivity" />
        <activity android:name=".PreBraincruncherActivity" />
        <activity android:name=".BraincruncherEnd" />
        <activity android:name=".BraincruncherEnd2"></activity>
    </application>

</manifest>